<% content_for :title, t("layout.page_title", title: t("home.index.title"), app: t("app.name")) %>
<section class="space-y-6">
  <p class="text-sm font-medium uppercase tracking-widest text-muted">StarterApp</p>
  <h1 class="text-4xl font-semibold tracking-tight"><%= t("home.index.heading") %></h1>
  <p class="max-w-xl text-lg text-muted"><%= t("home.index.intro") %></p>
  <%= link_to t("actions.open_profile"), account_path, class: "inline-flex rounded-xl bg-accent px-5 py-3 font-medium text-white" %>
</section>
