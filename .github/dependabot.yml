version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule: { interval: weekly }
    # AgentPrism upstream uses v3 PanelGroup/PanelResizeHandle; see docs/agents.md.
    ignore:
      - dependency-name: react-resizable-panels
        update-types: ["version-update:semver-major"]
    groups:
      agent-prism:
        patterns: ["react", "react-dom", "@types/react*", "@radix-ui/*", "react-json-pretty", "react-resizable-panels", "lucide-react"]
      tailwind:
        patterns: ["tailwindcss", "@tailwindcss/*"]
      herb:
        patterns: ["@herb-tools/*"]
  - package-ecosystem: bundler
    directory: /
    schedule: { interval: weekly }
  - package-ecosystem: github-actions
    directory: /
    schedule: { interval: weekly }
  - package-ecosystem: gradle
    directory: /native/android
    schedule: { interval: weekly }
    groups:
      android-build:
        patterns: ["com.android.*", "org.jetbrains.kotlin:*", "gradle-wrapper"]
  - package-ecosystem: docker
    directory: /
    schedule: { interval: weekly }
    # Keep Node on LTS; update a new major together with mise.toml/package.json.
    ignore:
      - dependency-name: library/node
        update-types: ["version-update:semver-major"]
