interface:
  display_name: Lookbook — Figma
  short_description: Deliver Figma designs and verify token parity
  default_prompt: Use $lookbook-figma to deliver Figma designs and verify token parity for StarterApp, using actual Rails/ViewComponent sources and verified previews.
policy:
  allow_implicit_invocation: true
