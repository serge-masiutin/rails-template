interface:
  display_name: Lookbook — Health
  short_description: Audit token adoption and design-system health
  default_prompt: Use $lookbook-health to audit token adoption and design-system health for StarterApp, using actual Rails/ViewComponent sources and verified previews.
policy:
  allow_implicit_invocation: true
