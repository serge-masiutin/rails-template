interface:
  display_name: Lookbook — Audit
  short_description: Review catalog drift and pending design decisions
  default_prompt: Use $lookbook-audit to review catalog drift and pending design decisions for StarterApp, using actual Rails/ViewComponent sources and verified previews.
policy:
  allow_implicit_invocation: true
