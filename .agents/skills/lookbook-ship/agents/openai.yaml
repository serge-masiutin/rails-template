interface:
  display_name: Lookbook — Ship
  short_description: Graduate an experiment while preserving its history
  default_prompt: Use $lookbook-ship to graduate an experiment while preserving its history for StarterApp, using actual Rails/ViewComponent sources and verified previews.
policy:
  allow_implicit_invocation: true
