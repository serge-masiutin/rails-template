#!/usr/bin/env ruby
require "securerandom"
require "yaml"

Dir.chdir File.expand_path("..", __dir__) do
  case ARGV
  when [ "setup" ]
    path = "config/anycable.local.yml"
    unless File.exist?(path)
      File.open(path, File::WRONLY | File::CREAT | File::EXCL, 0o600) do |file|
        file.write({ "secret" => SecureRandom.hex(32) }.to_yaml)
      end
    end
    puts "Local AnyCable configured: #{path}. The secret is excluded from Git and Docker."
  when [ "start" ]
    # Rails and Go share one local secret; its value stays out of process arguments.
    ENV["ANYCABLE_SECRET"] ||= YAML.safe_load_file("config/anycable.local.yml").fetch("secret")
    exec "docker", "compose", "run", "--rm", "--no-deps", "--service-ports", "anycable"
  else
    abort "Usage: bin/cable setup | start"
  end
end
