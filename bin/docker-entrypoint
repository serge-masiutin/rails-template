#!/bin/bash -e

# The web container prepares schemas; workers must not rerun migrations.
if [ "${*: -2:1}" == "./bin/rails" ] && [ "${*: -1:1}" == "server" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
