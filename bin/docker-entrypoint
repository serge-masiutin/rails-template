#!/bin/bash -e

# The web container prepares schemas; separate workers do not rerun migrations.
if [ "${*: -2:1}" == "./bin/rails" ] && [ "${*: -1:1}" == "server" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
