#!/bin/bash -e

# Схемы готовит web-контейнер; отдельный worker не запускает миграции повторно.
if [ "${*: -2:1}" == "./bin/rails" ] && [ "${*: -1:1}" == "server" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
